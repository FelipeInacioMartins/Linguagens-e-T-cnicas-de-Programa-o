#include <stdio.h>
#include <stdlib.h>
#include <math.h>

	int main(int argc, char *argv[]) {
	
	// Exercício 1 - Lista 1 )  Faça um programa que leia dois números inteiros e depois os imprima na ordem inversa em que eles foram lidos.

		int n1, n2, aux;
	
		printf("Digite um numero inteiro: ");
		scanf("%d", &n1);
		printf("Digite outro numero inteiro: ");
		scanf("%d", &n2);
		
		aux = n1; // Trocar valores na memória. Variável auxiliar para não perder o valor. ordenação
		n1 = n2; 
		n2 = aux;	
		
		printf("Os numeros inteiros na ordem inversa e: %d e %d", n1, n2);

	
	//  Exercício 2 - Lista 1) Faça um programa que leia um valor do tipo double e depois o imprima na forma de notação científica.
	
		double num_nc;
		
		printf("\nDigite um numero inteiro para converte-lo em notacao cientifica: ");
		scanf("%f", &num_nc);
		// Fazendo ainda!
	
	// Exercício 3 - Lista 1)  Implemente um programa que leia um número n [com n positivo & n <= 64] e mostre na tela o seu valor em base binária (bits). 

		int n, resultado, bit_64, bit_32, bit_16, bit_8, bit_4, bit_2;
	
		printf("Digite o valor para a conversao em binario: ");
		scanf("%d", &n);
	
		bit_64 = n % 2;
		resultado = n/2;
	
		bit_32 = resultado % 2;
		resultado = resultado/2;
		
		bit_16 = resultado % 2;
		resultado = resultado/2;
		
		bit_8 = resultado % 2;
		resultado = resultado/2;
		
		bit_4 = resultado % 2;
		resultado = resultado/2;
		
		bit_2 = resultado % 2;
		resultado = resultado/2;
		
	
		printf("O resultado da conversao %d em binario eh = %d%d%d%d%d%d%d", n, resultado%2, bit_2, bit_4, bit_8, bit_16, bit_32, bit_64);


	// Exercício 4 - Lista 1) (URI 1009) Faça um programa que leia, o salário fixo e o valor total em vendas de um vendedor.
	
		float salario_fixo, valor_vendas, comissao, valor_total;
	
		printf("\nQuanto voce recebe de salario fixo?: ");
		scanf("%f", &salario_fixo);
		printf("\nE qual foi o valor total das vendas?: ");
		scanf("%f", &valor_vendas);
	
		comissao = 0.15 * valor_vendas;
		valor_total = salario_fixo + valor_vendas + comissao;
	
		printf("\nO valor total que voce ira receber e: %.2f", valor_total);
	
	// Exercício 5 - Lista 1) Elabore um programa que peça ao usuário para digitar 4 valores. E mostre na tela a soma, a média e o produtório desses valores.
		
    	float a,b,c,d, media;
	 	int soma, produto;
		
	   printf("\nSoma, media e o produtorio de quatro valores.");
		printf("\nInsira o primeiro valor: ");
		scanf("%f", &a);
		
		printf("Insira o segundo valor: ");
		scanf("%f", &b);
		
		printf("Insira o terceiro valor: ");
		scanf("%f", &c);
		
		printf("Insira o quarto valor: ");
		scanf("%f", &d);
		
		soma = a+b+c+d;
		media = (a+b+c+d)/4;
		produto = a*b*c*d;
		
		printf("A soma %d, A media %.2f e o produtorio %d dos valores inseridos.", soma, media, produto);
		
	// Exercicio 6 - Lista 1)  Leia um valor inteiro correspondente à idade de uma pessoa em dias e informe-a em anos, meses e dias
		
		int idade_dias, anos, dias_sobrados, meses, dias;
		
		printf("\nInformar a idade de uma pessoa em dias e em anos, meses e dias.");
		
		printf("\nInforme a sua idade em dias: ");
		scanf("%d", &idade_dias);
		
		anos = idade_dias / 365;
		dias_sobrados = idade_dias - (anos*365);
		meses = dias_sobrados / 30;
		dias =  dias_sobrados - (meses*30);
		
		printf("A sua idade em anos, meses e dias eh: %d anos, %d meses e %d dias", anos, meses, dias);
		
		
		
	// 	Exercicio 7) Faça um programa que calcule e mostre o volume de uma esfera sendo fornecido o valor de seu raio (R). 
    // A fórmula para calcular o volume é: (4/3) * pi * R3. Considere (atribua) para pi o valor 3.14159.
		
		float raio, pi, volume;
		
		pi = 3.14159;
		
		printf("\nCalcular o volume de uma esfera.");
		
		printf("\nQual o valor do raio?: ");
		scanf("%f", &raio);
		
		volume = (4/3.0)*pi*pow(raio, 3);
		
		printf("O volume da esfera eh %.2f", volume);
		
		
	// Exercício 8 - Lista 1) Leia os quatro valores do usuário correspondentes às coordenadas em um plano cartesiano, p1(x1,y1) e p2(x2,y2).
    //calcule e mostre a distância entre eles, aplicando a distância euclidiana.

		float x1, x2, y1, y2, dist, p1, p2;
		
		printf("\nDistancia euclidiana: ");
		printf("\nInsira as coordenadas do ponto P1: ");
		scanf("%f %f", &x1, &y1);
		
		printf("Insira as coordenadas do ponto P2: ");
		scanf("%f %f", &x2, &y2);
		
		p1 = pow(x2 - x1, 2);
		p2 = pow(y2 - y1, 2);
		
		dist = sqrt(p1+p2);
		
		printf("Distancia (%.2f)", dist);
	
		
	// Exercício 10 Lista 2) Faça um programa que leia três valores e apresente o maior dos três valores lidos seguido da mensagem “eh o maior”.  
	
		int a1, b1, c1, d1, maior, maior_temp1, maior_temp2;
		
		printf("\nInforme quatro valores para serem comparados: ");
		scanf("%d %d %d %d", &a1, &b1, &c1, &d1);
		
		// conta - recursividade em C
		
		maior_temp1 = (a1+b1+abs(a1-b1))/2;
		maior_temp2 = (maior_temp1+c1+abs(maior_temp1-c1))/2;
		maior = (maior_temp2+d1+abs(maior_temp2-d1))/2;
		
		
		printf("Entre |%d|%d|%d|%d|, o |%d| eh maior.", a1,b1,c1,d1, maior);

	return 0;
}
